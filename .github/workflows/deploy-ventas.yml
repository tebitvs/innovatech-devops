name: Deploy Ventas API Innovatech

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "back-Ventas_SpringBoot/**"
      - ".github/workflows/deploy-ventas.yml"

permissions:
  contents: read

jobs:
  build-push-deploy:
    runs-on: ubuntu-latest

    env:
      IMAGE_TAG: ${{ github.sha }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build ventas image
        run: |
          docker build \
            -t ${{ secrets.ECR_REGISTRY }}/${{ secrets.ECR_REPO_VENTAS }}:${IMAGE_TAG} \
            -t ${{ secrets.ECR_REGISTRY }}/${{ secrets.ECR_REPO_VENTAS }}:latest \
            ./back-Ventas_SpringBoot/Springboot-API-REST

      - name: Push ventas image to ECR
        run: |
          docker push ${{ secrets.ECR_REGISTRY }}/${{ secrets.ECR_REPO_VENTAS }}:${IMAGE_TAG}
          docker push ${{ secrets.ECR_REGISTRY }}/${{ secrets.ECR_REPO_VENTAS }}:latest

      - name: Setup kubectl
        uses: azure/setup-kubectl@v4

      - name: Update kubeconfig
        run: |
          aws eks update-kubeconfig \
            --region ${{ secrets.AWS_REGION }} \
            --name ${{ secrets.EKS_CLUSTER_NAME }}

      - name: Deploy ventas API to EKS
        run: |
          kubectl set image deployment/ventas-api \
            ventas-api=${{ secrets.ECR_REGISTRY }}/${{ secrets.ECR_REPO_VENTAS }}:${IMAGE_TAG} \
            -n ${{ secrets.K8S_NAMESPACE }}

          kubectl rollout status deployment/ventas-api \
            -n ${{ secrets.K8S_NAMESPACE }}